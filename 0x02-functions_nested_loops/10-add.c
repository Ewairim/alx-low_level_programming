#include <unistd.h>
#include "main.h"
#include <stdio.h>
/**
 * add - add two integers
 * @a: parameter used
 * @b: parameter used
 * Return: returns (result)
 */
int add(int a, int b)
{
	int result;

	result = a + b;
	return (result);
}
