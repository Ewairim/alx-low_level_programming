#include <unistd.h>
#include <stdio.h>
#include "main.h"
/**
 * mul - multiplies two integers
 * @a: parameter used
 * @b: parameter used
 * Return: result
 */
int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
