#include <stdio.h>
/**
 * main - entry point
 * @argc: number of arguments
 * @argv: pointer
 * Return: 0 (success)
 */
int main(int argc, char **argv)
{
	if (argc == 1)
	{
		printf("%s\n", argv[0]);
	}
	return (0);
}
